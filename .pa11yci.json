{
  "defaults": {
    "standard": "WCAG2AA",
    "wait": 300,
    "timeout": 60000,
    "chromeLaunchConfig": {
      "args": ["--no-sandbox", "--disable-setuid-sandbox"]
    }
  },
  "urls": ["./src/index.html"]
}
